package org.aksw.jenax.io.json.accumulator;

/** Base interface for objects and fragment bodies, i.e. accumulators for a fixed set of fields. */
public interface AccJsonObjectLike
    extends AccJsonNode
{
}
