package org.aksw.jenax.graphql.sparql.v2.context;

/** If for a field being visited there is no root marker in the parent context then that field must be a root field. */
// XXX Perhaps there is a more idiomatic way?
public class RootFieldMarker {}
